# Task 08 — Model comparison (Week 2, Day 3)

**Goal:** MLP vs LSTM, honestly, side by side.

**Context:** day-3 notebook saved `dashboard/data/model_compare.json`.

**Prompt (backend):**
> Add `/compare` returning `dashboard/data/model_compare.json`.

**Prompt (frontend):**
> Add a small panel showing the two test-loss numbers as labeled bars. Title:
> "MLP vs LSTM (test loss — lower is better)".

**Verify:** the fancier model isn't obviously better. That's the point.
