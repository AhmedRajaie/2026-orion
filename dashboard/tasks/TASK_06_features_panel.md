# Task 06 — Feature panel (Week 2, Day 1)

**Goal:** make the model's input visible — show a stock's features.

**Context:** the day-1 notebook saved `dashboard/data/features.json`.

**Prompt (backend):**
> Add `/features` that reads `dashboard/data/features.json` and returns it as-is.

**Prompt (frontend):**
> Add a panel that fetches `/features` and shows each feature name with its value
> as a small horizontal bar. Label it "Model input (features)".

**Verify:** you can see the numbers the model will learn from.
