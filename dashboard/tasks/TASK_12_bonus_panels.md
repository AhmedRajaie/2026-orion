# Task 12 — Bonus panels (optional)

Two datasets your notebooks already save but no panel shows yet. Add them if you
want a richer dashboard.

## Pivot evidence (Week 2, Day 4)
`dashboard/data/predictor_equity.json` holds the predictor's equity curve vs the
benchmark — the visual proof that a decent predictor still loses.
> Add `/predictor` returning that file, and a panel plotting the two lines. Put it
> NEXT TO the loss panel — good predictions on one side, bad portfolio on the other.

## The Q-agent (Week 3, Day 1)
`dashboard/data/qagent.json` holds the tiny Q-table agent vs buy-and-hold.
> Add `/qagent` and a small panel plotting agent vs buy-and-hold. A nice "RL you can
> read" exhibit for demo day.
